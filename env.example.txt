# =============================================================================
# School Portal - Environment Variables Template
# =============================================================================
# Copy this file to .env and update the values
# NEVER commit .env to version control!
# =============================================================================

# Django Settings
# Generate SECRET_KEY with: python3 -c 'import secrets; print(secrets.token_urlsafe(50))'
SECRET_KEY=CHANGE-THIS-GENERATE-A-NEW-SECRET-KEY
DEBUG=False
ALLOWED_HOSTS=your-server-ip,localhost,127.0.0.1

# Database (PostgreSQL)
DB_NAME=school_portal
DB_USER=postgres
DB_PASSWORD=CHANGE-THIS-TO-STRONG-PASSWORD
DB_HOST=db
DB_PORT=5432

# JWT Settings
JWT_ACCESS_TOKEN_LIFETIME_MINUTES=60
JWT_REFRESH_TOKEN_LIFETIME_DAYS=7

# Email Settings (for Phase 3 notifications)
# Use console backend for development, SMTP for production
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=noreply@schoolportal.com

# Redis (for caching, Celery, and WebSockets)
REDIS_URL=redis://:CHANGE-THIS-PASSWORD@redis:6379/0
REDIS_PASSWORD=CHANGE-THIS-PASSWORD

# Celery (Phase 3 - Background Tasks)
CELERY_BROKER_URL=redis://:CHANGE-THIS-PASSWORD@redis:6379/0
CELERY_RESULT_BACKEND=redis://:CHANGE-THIS-PASSWORD@redis:6379/0

# CORS and CSRF
# For HTTP (testing): http://your-server-ip
# For HTTPS (production): https://your-domain.com
CORS_ALLOWED_ORIGINS=http://your-server-ip,http://localhost:3000
CSRF_TRUSTED_ORIGINS=http://your-server-ip,http://localhost

# Security Settings
# Set to True when using HTTPS/SSL in production
SECURE_SSL_REDIRECT=False
SESSION_COOKIE_SECURE=False
CSRF_COOKIE_SECURE=False

# AWS S3 (Optional - for production file storage)
USE_S3=False
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_STORAGE_BUCKET_NAME=
AWS_S3_REGION_NAME=us-east-1

# Twilio (Optional - for Phase 3 SMS notifications)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# =============================================================================
# Quick Setup Commands:
# =============================================================================
# 1. Copy this file: cp env.example.txt .env
# 2. Generate SECRET_KEY: python3 -c 'import secrets; print(secrets.token_urlsafe(50))'
# 3. Update passwords (DB_PASSWORD, REDIS_PASSWORD)
# 4. Update ALLOWED_HOSTS with your server IP
# 5. Update CORS_ALLOWED_ORIGINS and CSRF_TRUSTED_ORIGINS
# =============================================================================

